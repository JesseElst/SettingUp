const ZoekComponent = () => {
  const template = `
      <section class="zoekcomponent"> 
     <input type="text" placeholder="Bewaarde zoekcritium of een query uit de zoekgeschiedenis...">
      </section>`;
  return template;
};
export default ZoekComponent;
